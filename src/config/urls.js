const baseURL = 'https://jsonplaceholder.typicode.com/';

const urls = {
    user: '/users',
    todos: '/todos',
    albums: '/albums',
    comments: '/comments',
    posts : '/posts'
}
export {baseURL, urls}
